#include "Frame.h"

state_t
Frame::upButtonPressed(state_t currentState)
{
    return currentState;
}

state_t
Frame::upButtonReleased(state_t currentState)
{
    return currentState;
}

state_t
Frame::downButtonPressed(state_t currentState)
{
    return currentState;
}

state_t
Frame::downButtonReleased(state_t currentState)
{
    return currentState;
}

state_t
Frame::leftButtonPressed(state_t currentState)
{
    return currentState;
}

state_t
Frame::leftButtonReleased(state_t currentState)
{
    return currentState;
}

state_t
Frame::rightButtonPressed(state_t currentState)
{
    return currentState;
}

state_t
Frame::rightButtonReleased(state_t currentState)
{
    return currentState;
}

state_t
Frame::enterButtonPressed(state_t currentState)
{
    return currentState;
}

state_t
Frame::enterButtonReleased(state_t currentState)
{
    return currentState;
}

state_t
Frame::cupsChanged(state_t currentState, unsigned int)
{
    return currentState;
}

